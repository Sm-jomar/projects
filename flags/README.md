# Flag reports

The army builder can auto-commit flag reports here via the Cloudflare Worker in `/worker`.
Each file is a JSON export of units the user flagged as having wrong data
(points, faction, card image, etc.). Review and delete after applying fixes.
