# Public (non-secret) build-time config read by Vite during `vite build`.
# These values are embedded into the client bundle, so never put secrets here.
# The flag backend Worker the app POSTs flag exports to.
VITE_FLAG_ENDPOINT=https://wrangler.sm-af6.workers.dev
